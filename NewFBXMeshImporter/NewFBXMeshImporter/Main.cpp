#include "FBXExporter.h"


// You can import either the model or the animation first
// Clear the eporter after every model change(model and animation set)
// Animations store bind poses
int main()
{
	FBXMeshExporter exporter;
	exporter.Initialize();





	return 0;
}